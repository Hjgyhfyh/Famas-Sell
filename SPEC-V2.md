# SPEC V2 — дизайн-система, темы, флаги, цена (дополнение к SPEC.md)

V2 ПЕРЕКРЫВАЕТ §11 SPEC.md (дизайн) и уточняет цену. Контракты модулей/БД/маршрутов из SPEC.md
остаются в силе. Всё на русском. Приоритет №1 — ПРОСТОТА и удобство покупки (чем проще, тем больше
продаж): меньше шагов, крупные кнопки, никакой перегрузки.

## 1. Цена

Дефолтная цена: **20 ⭐ за 1 страну** (`DEFAULT_PRICE_STARS=20` в config.js и .env.example).

## 2. Флаги — SVG-иконки, НЕ эмодзи

- В `public/flags/` лежат 271 SVG (набор flag-icons 4x3, имена: `de.svg`, `nl.svg`, — lowercase ISO).
- server.js отдаёт их: `GET /famas/flags/<file>` → `express.static(public/flags, { maxAge: '30d', immutable: true })`.
- Во ВСЁМ фронтенде (mini app, key.html) флаг = `<img class="flag" src="/famas/flags/<iso_lower>.svg">`
  в скруглённом чипе (соотношение 4:3, border-radius 6–8px, тонкая обводка var(--border),
  лёгкая внутренняя тень для глубины). `onerror` → скрыть img, показать эмодзи-фолбэк (поле flag из API).
- В сообщениях БОТА остаются эмодзи-флаги (Telegram не умеет иначе).

## 3. Дизайн-система v2 «FAMAS ROUNDED»

Всё **закруглённое**: `--r-sm:10px; --r-md:16px; --r-lg:22px; --r-xl:28px`, кнопки-пилюли
(border-radius 999px) для чипов/тостов. Никаких острых углов.
Типографика прежняя: серифы (Georgia) для крупных заголовков/бренда, моно для цифр/меток/ссылок,
системный sans для UI. Капс с letter-spacing .12em для секций. Знак бренда ⁂, индексы 01/ 02/.

### Темы (CSS custom properties на `:root[data-theme=...]`)

**bw (дефолт)** — фирменный чёрно-белый:
```
--bg:#000; --bg2:#0a0a0a; --card:#111214; --card2:#1a1b1e; --border:#26272b;
--text:#fff; --muted:#8a8a8a; --accent:#fff; --accent-contrast:#000;
--good:#fff (точка статуса пульсирует белым); --dim:#666;
--pay-bg:#fff; --pay-text:#000;
```
**dracula** — палитра Dracula (исходник владельца):
```
--bg:#191A21; --bg2:#21222C; --card:#282A36; --card2:#353849; --border:#44475A;
--text:#F8F8F2; --muted:#6272A4; --accent:#BD93F9; --accent2:#FF79C6;
--good:#50FA7B; --info:#8BE9FD; --dim:#6272A4;
--pay-bg: linear-gradient(135deg,#BD93F9,#FF79C6); --pay-text:#191A21;
```
Тема применяется ДО первого рендера (инлайн-скрипт в `<head>` читает localStorage) — без вспышки.
Переключение — плавный кроссфейд ~250ms (transition на цветовых свойствах через класс на body).

### Жидкое стекло (toggle, атрибут `data-glass="1"` на :root)

Выключено — плоские карточки. Включено:
- карточки/панели/табы: полупрозрачный фон (bw: rgba(20,20,22,.55); dracula: rgba(40,42,54,.55)),
  `backdrop-filter: blur(20px) saturate(160%)` (+ `-webkit-backdrop-filter`!), обводка
  rgba(255,255,255,.12), верхний блик `inset 0 1px 0 rgba(255,255,255,.16)`;
- позади контента — 3-4 больших размытых «блоба» (fixed, blur(90px), медленный дрейф
  transform-анимацией 40-70s, `prefers-reduced-motion` — отключить): bw → белые/серые с opacity
  .05–.08; dracula → #BD93F9/#FF79C6/#8BE9FD с opacity .10–.16;
- фолбэк без backdrop-filter (@supports) — просто полупрозрачный фон потемнее.

### Настройки (в mini app — отдельный экран/лист)

Кнопка ⚙ в шапке + вкладка/пункт «НАСТРОЙКИ» открывает **bottom-sheet** (скруглённый r-xl сверху,
ручка-полоска, слайд-ап 300ms, бэкдроп-затемнение):
- «ТЕМА» — сегмент-контрол [ ЧБ | DRACULA ] (скруглённый, бегунок анимирован);
- «ЖИДКОЕ СТЕКЛО» — iOS-переключатель (анимированный кноб);
- живое мини-превью карточки под контролами.
Хранение: localStorage `famas_theme` ('bw'|'dracula'), `famas_glass` ('0'|'1') +
зеркало в `Telegram.WebApp.CloudStorage` (setItem fire-and-forget; при старте getItems с
фолбэком на localStorage; все вызовы в try/catch — вне Telegram их нет).
key.html: та же тем-система; в правом верхнем углу квитанции компактный переключатель
(две точки-темы + иконка стекла), хранение в localStorage тех же ключей.

### Анимации (обязательный минимум, все — с prefers-reduced-motion фолбэком)

mini app: staggered fade-up карточек при первом рендере (delay i*40ms, 340ms,
cubic-bezier(.22,1,.36,1)); слайд-индикатор активной вкладки; кастомный чекбокс с пружинкой
(scale 1→1.15→1) и прорисовкой галочки; press-feedback scale(.97) на всех кнопках; нижняя
панель оплаты выезжает при выборе ≥1 региона; каунт-ап суммы; спиннер в кнопке при создании
инвойса; экран успеха — SVG-галочка с прорисовкой штриха + поп карточки; тосты-пилюли
слайд+фейд; skeleton-шиммер при загрузке регионов; кроссфейд смены темы.
key.html: квитанция «падает» на место (drop-in + лёгкий поворот, settle 500ms); QR проявляется;
точка статуса пульсирует; кнопка копирования — рипл + морф текста «СКОПИРОВАНО ✓»; аккордеон
инструкции с плавным раскрытием.

### Усиление удобства (конверсия)

- Карточка региона кликается ЦЕЛИКОМ (не только чекбокс), большая зона тапа, haptic
  (Telegram.WebApp.HapticFeedback.impactOccurred('light') в try/catch).
- Если регионов > 12 — сверху поле-фильтр (пилюля, моно, «поиск региона…»).
- Чипы «✦ Выбрать всё» / «✕ Сброс» над сеткой.
- В шапке магазина строка доверия: «● N серверов · M стран · обновлено HH:MM» (живые данные).
- Пустые состояния и ошибки — дизайнерские (⁂, мягкий текст, кнопка-действие).
- Успех: «ключ уже в чате с ботом» + кнопки «ОТКРЫТЬ СТРАНИЦУ КЛЮЧА» и «В МОИ КЛЮЧИ».

## 4. Бекенд-дельты V2 (минимальные)

1. `src/server.js`: добавить статик-раздачу `/famas/flags` (cache 30d, immutable) ДО дефолтного 404.
2. `src/config.js` + `.env.example`: DEFAULT_PRICE_STARS=20.
3. Ничего больше в контрактах не менять.

## 5. Критерии приёмки V2

1. Обе темы полноценные, переключение без перезагрузки и без «вспышки» при старте.
2. Стекло работает в обеих темах (и явный фолбэк без backdrop-filter).
3. Все флаги — SVG-иконки со скруглением; эмодзи-фолбэк работает.
4. 60fps: анимации только transform/opacity (без layout-трэша), will-change точечно.
5. Работает в Telegram WebView iOS/Android И в обычном браузере (key.html — обязательно в браузере).
6. Цена по умолчанию 20⭐, в UI цена приходит из API (не хардкод).
7. Простота: покупка = открыл → тапнул страны → «ОПЛАТИТЬ» → готово (3 действия).
